interface:
  display_name: "SE"
  short_description: "Repo-specific patterns and workflows for SE"
  default_prompt: "Use the SE repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true