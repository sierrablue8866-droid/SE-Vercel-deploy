# ═══════════════════════════════════════════════════════════════════════════
# Sierra Estates — Infrastructure .gitignore
# ═══════════════════════════════════════════════════════════════════════════

# 🔒 Secrets — NEVER commit these
.env
.env.local
.env.production
secrets/
*.json
!package.json
!tsconfig.json
!firebase-blueprint.json

# 📦 n8n data (workflows + credentials)
n8n-data/

# 📱 WhatsApp auth session
whatsapp-auth/

# 🔧 Node
node_modules/
*.log
npm-debug.log*

# 🖥️ OS
.DS_Store
Thumbs.db
